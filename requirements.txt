# Finlingo Vercel Python functions use only the Python standard library
# (urllib, json, sqlite3, http.server). No third-party packages are required
# at runtime, so this file is intentionally empty.
#
# The previous matplotlib/numpy/scipy list (a leftover local virtualenv freeze
# that nothing in this repo imports) was moved to requirements-local.txt to keep
# the serverless function bundles small and builds fast.
